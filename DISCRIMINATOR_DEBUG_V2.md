# ORE Automate Instruction Discriminator - Alternative Approach

## Current Status
Still getting `0x65` error (InstructionFallbackNotFound) even with Anchor-style discriminators.

## New Approach: Without "global:" Prefix

Based on ore-cli usage:
- ore-cli uses `ore_api::sdk::automate()` directly
- SDK calls `Automate { ... }.to_bytes()` 
- The `.to_bytes()` method is generated by steel's `instruction!` macro

**Key Insight**: Maybe steel framework doesn't use the "global:" prefix even though it shows "AnchorError".

### Trying Discriminators WITHOUT "global:" Prefix

- **Automate**: `SHA256("Automate")[:8]` = `f9dcbb55deba4b9f`
- **ClaimSOL**: `SHA256("ClaimSOL")[:8]` = `b4abeab608075153`
- **Deploy**: `SHA256("Deploy")[:8]` = `4c236daafb445f86`
- **ClaimORE**: `SHA256("ClaimORE")[:8]` = `0078fc141c6cabb9`

### Previous Attempts

1. ✅ With "global:" prefix: `SHA256("global:Automate")[:8]` = `ed7408425064bd96` ❌ Failed
2. ✅ Lowercase: `SHA256("global:automate")[:8]` = `546aae5e07401682` ❌ Failed
3. ✅ Function name: `SHA256("global:process_automate")[:8]` = `8493ea04d539f386` ❌ Failed
4. ✅ 1-byte enum value: `[0x00]` ❌ Failed
5. 🆕 **Without "global:" prefix**: `SHA256("Automate")[:8]` = `f9dcbb55deba4b9f` ⏳ Testing

## Next Steps

If this still fails, we need to:
1. Find a real on-chain Automate transaction and extract the exact discriminator
2. Check steel framework source code to see how `instruction!` macro actually works
3. Use ore-cli to generate a transaction and analyze it

